[project]
name = "wisconsin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "qwen-asr>=0.0.6",
    "tqdm>=4.67.3",
]

[tool.poe.tasks]
transcribe = "uv run python transcribe.py"

[dependency-groups]
dev = [
    "poethepoet>=0.42.1",
    "ruff>=0.15.5",
]
